ECE 6102 Project- Network Stack functions.

To use mysend(void* data, char* ) function place the files UDPclient.c and UDPclient.h in your directory and include "UDPclient.h"
in your file.

To use void* myrecv() function place the files UDPserver.c and UDPerver.h in your directory and include "UDPserver.h" in your file. 

Please check the client.c and server.c files to see usage. 

Currently, these functions can send easily send the proposed messages between the servers in out DFS.


The content of makefile_temp needs to be added to the primary makefile.
